import React, { Component } from 'react';

const spacing = {
    margin: "10px",
    padding: "10px",
    textAlign: "center"
}

const Product = (props) => {

    return(
        <h1 style={spacing}>Thank You for your purcahse :)</h1>
    );
}

export default Product; 